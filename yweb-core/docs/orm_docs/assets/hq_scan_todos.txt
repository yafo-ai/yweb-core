Phase 8.4 — 清理 HybridQuery / run_db / async_db_call / AsyncSafeQueryProperty 相关 TODO
扫描时间: 2026-04-21
扫描命令:
  rg -i "TODO.*(HybridQuery|run_db|async.*query|AsyncSafeQueryProperty|async_db_call)" yweb tests

扫描范围: yweb-core/yweb/** + yweb-core/tests/**（生产代码 + 测试）

========================================
命中结果: 0 条（生产代码 + 测试）
========================================

分析:
- 本次 Phase 0 ~ Phase 8.5 的所有实施阶段没有留下任何 TODO / FIXME
- async-safety 引入（b1434c5）、API 路由迁移（0a5e5a8）、OIDC 修复
  （8743d1a）、JWT kid 特性（7e5f887）、run_db → async_db_call 改名
  （ff97dc6）全部是「完成闭环」，没有延期到未来某个 TODO 的悬挂项
- mixins 时区 bug 修复（8b1b81a）也是完整闭环

命中分类（文档/清单里的 TODO 描述性引用，非真实 TODO 代码）:
  - docs/orm_docs/23_hybrid_query_execution_checklist.md:280
    Phase 8.4 本条任务自身的动作描述 `rg "TODO.*(...)" yweb tests`
  - docs/orm_docs/23_hybrid_query_execution_checklist.md:281
    Phase 8.4 本条任务自身的验收标准
  这两条是元描述，不是代码 TODO。

========================================
清单内的占位符 [ ] 最终方案 = ___________________ 状态
========================================

定位: docs/orm_docs/23_hybrid_query_execution_checklist.md

1. Phase 3.3 "同步 / 异步双面" 决策占位（line 146 附近）
   `[ ] 最终方案 = ___________________`
   状态: 未决策。需要在 Phase 3.1 写 _HybridTerminal 时一并敲定：
     方案 A: 终端在同步上下文直接求值；async 上下文返回 awaitable（推荐）
     方案 B: 同步入口 .value() / .result()，async 必 await（语义更一致）
   处置: 保留占位，Phase 3 开工时填入。

2. Phase 7.3 回滚脚本 / 开关决策占位（line 244 附近）
   `[ ] 最终方案 = ___________________`
   状态: 已在清单决策表 D3 锁定为 A: `YWEB_HYBRID_QUERY=off` 环境变量
   处置: 需要把这个占位符改为 `[x] 最终方案 = A（YWEB_HYBRID_QUERY=off）`，
        避免占位符和已决策内容不一致。

========================================
结论
========================================

生产代码与测试代码 0 个待清理 TODO，清单内 2 个决策占位符:
  - Phase 3.3: 未决策（等 Phase 3 开工）
  - Phase 7.3: 已决策为 D3=A，占位符文本需要在下一 Phase 清单更新时同步

Phase 8.4 验收: 本扫描无残留 TODO；Phase 7.3 占位符同步为待落实事项。
