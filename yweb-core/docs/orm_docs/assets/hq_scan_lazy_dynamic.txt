Phase 0.1 — 扫描 lazy='dynamic' 关系
扫描时间: 2026-04-21
扫描命令: rg "lazy=['\"]dynamic['\"]" yweb-core/yweb
扫描范围: yweb-core/yweb/**（生产代码）

========================================
命中结果: 0 条
========================================

结论:
- 当前框架代码里没有任何使用 lazy='dynamic' 的关系定义。
- AppenderQuery 类型不存在于任何 Model 上。

对 HybridQuery 的含义:
- HybridQuery 暂不需要为 AppenderQuery 做特殊包装（doc 22 §7.3.5 的风险点在当前仓库不落地）。
- 若上游业务项目（如 y-sso-system）自定义了 lazy='dynamic' 关系，迁移时仍需在他们那一侧处理：
    * 方案 A：文档明示 AppenderQuery 在 async 中需 async_db_call 包装
    * 方案 B：HybridQuery 扩展支持（若命中量大再评估）
- Phase 5.3 中「lazy='dynamic' 覆盖」测试可在 test fixtures 里手工构造，不需为生产代码兜底。

处置: 无需改动任何生产代码。
