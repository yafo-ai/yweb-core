---
description: 编写数据库模型定义、CRUD 操作、查询过滤、分页、软删除、事务管理、批量操作、关系定义等数据层代码时自动激活。
globs: yweb-core/yweb/orm/**/*.py, **/domain/**/entities.py, **/domain/**/model/**/*.py, **/models_registry.py, **/database.py
---

# YWeb ORM 编码规范

编写或修改 ORM 相关代码时，**必须先阅读对应的 Skill 文档**：

> **Skill 路径**：`.cursor/skills/yweb-orm/SKILL.md`
>
> **DDD 分层补充**：`.cursor/rules/yweb-ddd-architecture.mdc`

## 关键规范速查

1. **所有字段必须有 `comment`**：`mapped_column(String(255), comment="字段说明")`
2. **使用 SQLAlchemy 2.0 风格**：`Mapped[str]` 类型注解 + `mapped_column()`
3. **继承选择**：`BaseModel`（含 id/name/code/时间戳/软删除）或 `CoreModel`（仅 id+时间戳）
4. **软删除**：`BaseModel` 自带，查询自动过滤已删除记录
5. **事务管理**：Service 层使用 `@transactional`，不在 Model 层管理事务
6. **Active Record 方法**：`.add()` / `.update()` / `.delete()` / `.get()` / `.get_all()` / `.query`
7. **模型层不关心 HTTP**：不返回 `Resp.*`、不抛 HTTP 相关异常
8. **async 读路径（推荐 HybridQuery）**：`Model.query` 已升级为 HybridQuery，**同步代码零改动**；`async def` 路由里**链式不变，终端加 `await`**：
   ```python
   users = await User.query.filter(User.is_active.is_(True)).all()
   first = await User.query.filter_by(email=email).first()
   page  = await User.query.order_by(User.id.desc()).paginate(page=1, size=20)
   ```
   支持的终端：`all / first / one / one_or_none / count / get / scalar / delete / update / paginate`
9. **async 写路径 / 多语句事务兜底**：涉及 `save() / commit() / add() / update() / delete()` 等写操作、或需要一次 session 内跑多语句时，用 `def` 路由（FastAPI 自动线程池）或 `await async_db_call(...)` 手动包装；启动/lifespan 中用 `with allow_sync():` 或 `db_session_scope()`（async 下可直接用，内部已自动 `allow_sync`）
10. **回滚开关**：环境变量 `YWEB_HYBRID_QUERY=off` 可切回旧行为（async 下 `.query.xxx()` 不返回 awaitable，而是原地抛 `SynchronousOnlyOperation`）

不确定时先阅读 `yweb-core/docs/03_orm_guide.md`。
